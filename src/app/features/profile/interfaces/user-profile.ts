// modify to include an id that will be used to identify 
// each profile as needed in the id route parameter
export interface UserProfile {
    cellNumber: string;
    city: string;
    dateOfBirth: string;
    email: string;
    firstName: string;
    id: number;
    lastName: string;
    phoneNumber: string;
    picture: string;
    state: string;
}
